version: '3.8'

services:
  # ------------------------------------------------------------------
  # [COMMENTED OUT] Local Docker PostgreSQL — Migrated to Supabase
  # To restore local DB, uncomment this entire block and the
  # 'postgres_data' volume at the bottom.
  # ------------------------------------------------------------------
  # db:
  #   image: pgvector/pgvector:pg16
  #   container_name: bank_postgres
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: abc123
  #     POSTGRES_DB: bank_db
  #   ports:
  #     - "5433:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:alpine
    container_name: bank_redis
    ports:
      - "6379:6379"

  # ------------------------------------------------------------------
  # [COMMENTED OUT] Local Docker MinIO — Migrated to Supabase Storage
  # To restore local MinIO, uncomment this entire block and the
  # 'minio_data' volume at the bottom.
  # ------------------------------------------------------------------
  # minio:
  #   image: minio/minio
  #   container_name: bank_minio
  #   command: server /data --console-address ":9001"
  #   environment:
  #     MINIO_ROOT_USER: minioadmin
  #     MINIO_ROOT_PASSWORD: minioadmin
  #   ports:
  #     - "9000:9000"
  #     - "9001:9001"
  #   volumes:
  #     - minio_data:/data

volumes:
  # postgres_data:   # Uncomment if reverting to local Docker PostgreSQL
  # minio_data:      # Uncomment if reverting to local Docker MinIO
